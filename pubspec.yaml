name: mail_command_center
description: Responsive mail workspace — Flutter web.
publish_to: "none"
version: 1.0.0+1
repository: https://github.com/napannich/mail-command-center

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
